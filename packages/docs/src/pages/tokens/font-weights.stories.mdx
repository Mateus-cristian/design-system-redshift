import { Canvas, Meta } from "@storybook/blocks";
import TokenGrid from "../../components/TokensGrid";

import { fontWeights } from "@redshift-ui/tokens";

<Meta title="Tokens/Font-Weights" />

# Font Weights

Font-Weights utilizadas no redshift

<TokenGrid tokens={fontWeights} hasRemValue={false} />
