import { Canvas, Meta } from "@storybook/blocks";
import TokenGrid from "../../components/TokensGrid";

import { space } from "@redshift-ui/tokens";

<Meta title="Tokens/Space" />

# Space

Spaces utilizados no redshift

<TokenGrid tokens={space} hasRemValue={true} />
